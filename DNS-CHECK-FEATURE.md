# DNS 解析檢查功能說明

## 📋 功能概述

在 Step 03（建立網站）執行前，自動檢查域名的 DNS 解析是否正確指向目標伺服器。

### 為什麼需要這個功能？

- **Step 02** 完成域名註冊和 DNS 設定
- **DNS 傳播** 需要時間（通常 5-30 分鐘）
- **Step 03** 建立網站前必須確保 DNS 已生效
- **避免錯誤** 防止在 DNS 未生效時建立網站導致配置問題

---

## 🎯 檢查邏輯

### 檢查內容
- **檢查域名**: `www.{domain}` (只檢查 www 子域名)
- **預期 IP**: `163.61.60.36` (可配置)
- **檢查方式**: 使用多種 DNS 查詢方法

### 檢查方法
1. **dns_get_record()** - PHP 原生函數，最準確
2. **gethostbyname()** - 備用方案

### 重試機制
- **預設重試次數**: 10 次
- **預設間隔**: 180 秒（3 分鐘）
- **最大等待時間**: 30 分鐘
- **全部可配置**: 透過 config 檔案調整

---

## 📝 配置方式

### 方法 1: 使用預設值（無需配置）

如果不設定，系統會使用以下預設值：
```json
{
    "target_ip": "163.61.60.36",
    "max_retries": 10,
    "retry_interval": 180
}
```

### 方法 2: 自訂配置

在 `config/deploy-config.json` 中加入：

```json
{
    "dns_check": {
        "target_ip": "163.61.60.36",
        "max_retries": 10,
        "retry_interval": 180
    }
}
```

### 配置參數說明

| 參數 | 說明 | 預設值 | 單位 |
|------|------|--------|------|
| `target_ip` | 預期的目標 IP 地址 | `163.61.60.36` | - |
| `max_retries` | 最大重試次數 | `10` | 次 |
| `retry_interval` | 每次重試間隔 | `180` | 秒 |

### 配置範例場景

#### 快速檢查（5 分鐘）
```json
{
    "dns_check": {
        "target_ip": "163.61.60.36",
        "max_retries": 5,
        "retry_interval": 60
    }
}
```

#### 標準檢查（30 分鐘）- 推薦
```json
{
    "dns_check": {
        "target_ip": "163.61.60.36",
        "max_retries": 10,
        "retry_interval": 180
    }
}
```

#### 耐心檢查（60 分鐘）
```json
{
    "dns_check": {
        "target_ip": "163.61.60.36",
        "max_retries": 20,
        "retry_interval": 180
    }
}
```

---

## 🧪 測試方式

### 1. 獨立測試腳本

使用提供的測試腳本來驗證 DNS 解析：

```bash
# 基本用法
php test-dns-check.php example.tw

# 指定預期 IP
php test-dns-check.php example.tw 163.61.60.36
```

### 2. 測試輸出範例

#### 成功的情況：
```
========================================
DNS 解析檢查測試
========================================
檢查域名: www.example.tw
預期 IP: 163.61.60.36
========================================

🔍 檢查方法 1: dns_get_record()
  ✅ 成功解析
  IP 地址: 163.61.60.36
  記錄數量: 1

========================================
檢查結果
========================================
狀態: ✅ 成功
解析 IP: 163.61.60.36
解析方法: dns_get_record

✅ IP 匹配成功！
   www.example.tw -> 163.61.60.36
   此域名的 DNS 已正確配置
```

#### 失敗的情況：
```
========================================
檢查結果
========================================
狀態: ❌ 失敗
錯誤: DNS resolution failed

可能的原因:
1. DNS 記錄尚未建立
2. DNS 傳播需要時間（通常 5-30 分鐘）
3. 域名配置錯誤
4. 網路連線問題
```

### 3. 手動驗證 DNS

使用系統命令驗證：

```bash
# 使用 dig（推薦）
dig www.example.tw @1.1.1.1

# 使用 nslookup
nslookup www.example.tw 1.1.1.1

# 使用 host
host www.example.tw
```

---

## 📊 運作流程

```
Step 02: 域名註冊
    ↓
    註冊域名到 Lihi
    ↓
    設定 Cloudflare DNS
    ↓
    建立 A 記錄: www -> 163.61.60.36
    ↓
    [DNS 傳播開始...]
    ↓
Step 03: 建立網站
    ↓
    ┌─────────────────────────────┐
    │ DNS 檢查迴圈 (最多 10 次)    │
    ├─────────────────────────────┤
    │ 1. 查詢 www.{domain}        │
    │ 2. 檢查 IP 是否 = 163.61.60.36 │
    │ 3. 如果不符合，等待 3 分鐘   │
    │ 4. 重複檢查                 │
    └─────────────────────────────┘
    ↓
    [DNS 驗證成功]
    ↓
    呼叫 Luke API 建立網站
    ↓
    網站建立完成
```

---

## 🔍 詳細日誌範例

### 執行中的日誌

```
🌐 ========================================
🌐 前置檢查：驗證 DNS 解析
🌐 ========================================
檢查目標: www.example.tw
預期 IP: 163.61.60.36
最大重試: 10 次
重試間隔: 180 秒 (3 分鐘)

📡 DNS 檢查嘗試 [1/10]
  檢查: www.example.tw
  解析結果: 104.21.45.123
  解析方法: dns_get_record
  ⚠️  IP 不符: 104.21.45.123 (預期: 163.61.60.36)

⏳ 等待 180 秒後重試...
  (已嘗試 1/10 次，剩餘 9 次)

📡 DNS 檢查嘗試 [2/10]
  檢查: www.example.tw
  解析結果: 163.61.60.36
  解析方法: dns_get_record

✅ DNS 解析驗證成功！
  www.example.tw -> 163.61.60.36
  耗時: 195 秒 (3.3 分鐘)
  總嘗試次數: 2 次

🌐 ========================================
🌐 DNS 驗證通過，繼續執行網站建立
🌐 ========================================
```

### 失敗的日誌

```
❌ DNS 解析驗證失敗
========================================
檢查域名: www.example.tw
預期 IP: 163.61.60.36
總嘗試次數: 10 次
總等待時間: 30.0 分鐘
實際耗時: 30.2 分鐘
========================================

可能的原因:
1. DNS 記錄尚未建立或配置錯誤
2. DNS 傳播需要更多時間（通常 5-30 分鐘）
3. 名稱伺服器配置錯誤

建議處理方式:
1. 檢查 Cloudflare DNS 設定是否正確
2. 使用 dig 或 nslookup 手動驗證: dig www.example.tw
3. 等待更長時間後重新執行此步驟
4. 調整 deploy-config.json 中的 dns_check.max_retries 或 retry_interval
```

---

## ❌ 常見問題與解決方案

### 問題 1: DNS 持續無法解析

**症狀**: 重試多次後仍然無法解析

**可能原因**:
- DNS 記錄未建立
- Cloudflare 配置錯誤
- 域名註冊失敗

**解決方案**:
1. 登入 Cloudflare 檢查 DNS 記錄
2. 確認 A 記錄存在：
   - 名稱: `www`
   - 類型: `A`
   - 內容: `163.61.60.36`
3. 檢查代理狀態（橘色雲朵）
4. 使用 `dig` 命令驗證：
   ```bash
   dig www.yourdomain.tw @1.1.1.1
   ```

### 問題 2: IP 地址不匹配

**症狀**: DNS 可以解析，但 IP 不是預期的

**可能原因**:
- A 記錄指向錯誤的 IP
- 使用了 CDN 或代理
- DNS 記錄尚未完全傳播

**解決方案**:
1. 檢查 Cloudflare A 記錄的內容
2. 暫時關閉 Cloudflare 代理（灰色雲朵）測試
3. 等待 DNS 完全傳播
4. 清除本地 DNS 緩存：
   ```bash
   # macOS
   sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder

   # Linux
   sudo systemd-resolve --flush-caches
   ```

### 問題 3: 檢查時間過長

**症狀**: 等待時間超過預期

**可能原因**:
- DNS 傳播較慢
- 重試次數設定過多

**解決方案**:
1. 調整配置減少等待時間：
   ```json
   {
       "dns_check": {
           "max_retries": 5,
           "retry_interval": 60
       }
   }
   ```
2. 先手動確認 DNS 後再執行 step-03
3. 使用測試腳本獨立驗證

---

## 📁 相關檔案

| 檔案 | 說明 |
|------|------|
| `step-03-luke.php` | 主要功能實作（已修改） |
| `test-dns-check.php` | 獨立測試腳本 |
| `config/dns-check-config.example.json` | 配置範例 |
| `DNS-CHECK-FEATURE.md` | 本說明文件 |

---

## 🚀 使用建議

### 開發/測試環境
- 使用較短的重試時間
- 先用測試腳本驗證 DNS
- 配置：5 次重試，60 秒間隔

### 生產環境
- 使用標準或較長的重試時間
- 確保 DNS 有充足時間傳播
- 配置：10-20 次重試，180 秒間隔

### 緊急情況
- 跳過 DNS 檢查（不推薦）
- 手動確認 DNS 後直接執行
- 或暫時修改程式碼註解掉檢查邏輯

---

## 📞 支援與回饋

如果遇到問題或有建議，請：

1. 執行測試腳本收集診斷資訊
2. 檢查 Cloudflare DNS 設定
3. 查看完整的執行日誌
4. 調整配置參數後重試

---

## 📝 更新日誌

### Version 1.0 (2026-01-30)
- ✅ 實作 DNS 解析檢查功能
- ✅ 支援可配置的重試機制
- ✅ 提供詳細的日誌輸出
- ✅ 建立獨立測試腳本
- ✅ 使用多種 DNS 查詢方法
- ✅ 友善的錯誤訊息和建議
